pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "ProjectCodeName"
include(
    ":app",
    ":app-domain",
    ":app-infrastructure",
    ":app-common"
)
